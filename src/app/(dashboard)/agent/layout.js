export const metadata = {
  title: 'AI Agent — Buzz',
  description: 'Your personal PM Coach, powered by Gemini AI.',
};

export default function AgentLayout({ children }) {
  return <>{children}</>;
}
